# TODO

- [ ] Fix auth-service DB connection (TypeORM) to use Docker env vars instead of hardcoded localhost/5433.
- [ ] Rebuild and restart docker platform (docker compose down && up --build).
- [ ] Verify: frontend login/register returns JSON (no ERR_EMPTY_RESPONSE).

